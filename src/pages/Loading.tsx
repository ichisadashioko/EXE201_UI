import Logo from "../components/common/Logo";

const Loading = () => {
  return (
    <section className="h-[100vh] bg-home flex flex-col gap-2 items-center justify-center">
      <Logo />
    </section>
  );
};

export default Loading;
